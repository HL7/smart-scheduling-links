== Suppressed Messages ==

# Add warning and/or information messages here after you've confirmed that they aren't really a problem
# (And include comments like this justifying why)
# See https://github.com/FHIR/sample-ig/blob/master/input/ignoreWarnings.txt for examples

# EDITOR NOTES (these lines are parsed but discarded - the IG Publisher publishes only the LAST
# "# " line before an entry as the reason shown in qa.html, so keep each published reason
# self-contained on one line). Entries wrapped in % are matched as "contains" rather than as an
# exact string, which stops them going stale when a version number or nearby wording changes.
# Do not use a bare "#" as a separator: without a trailing space it is parsed as a suppression
# entry rather than a comment.

# Not actionable in this IG: the maxValueSet extension is inherited unchanged from the FHIR R4 core definition of DomainResource.language, which appears as HealthcareService.language in the generated snapshot. No profile in this IG declares a maxValueSet binding of its own, and the replacement recommended by the deprecation note (ElementDefinition.binding.additional) does not exist in R4, so there is no change this IG can make.
%The extension http://hl7.org/fhir/StructureDefinition/elementdefinition-maxValueSet%

# EDITOR NOTES for the two CodeSystem/temporary entries below (FHIR-59379, FHIR-59433).
# CodeSystem/temporary was introduced under FHIR-59379 to replace this IG's former custom code
# system (location-physical-type-cs), and is current. Delete both entries only in a future release,
# after the ph code has been added to http://terminology.hl7.org/CodeSystem/location-physical-type
# and this IG has switched to that code. 

# Intentional, per TSMG (FHIR-59379): CodeSystem/temporary is a stopgap for the ph code until it is added to the existing THO code system http://terminology.hl7.org/CodeSystem/location-physical-type, so this code system is deliberately not being given a THO URL of its own. Discussed with TSMG at the September 2026 WGM, Monday Q4: https://confluence.hl7.org/spaces/VOC/pages/474742994/Sept+2026+-+HL7+WGM+-+Monday+Q4+Minutes
%Most code systems defined in HL7 IGs will need to move to THO later%

# Intentional, per TSMG (FHIR-59379): CodeSystem/temporary is marked experimental on purpose, to signal that the ph code used by the Location example will change when it is added to HL7 Terminology (THO). Discussed with TSMG at the September 2026 WGM, Monday Q4: https://confluence.hl7.org/spaces/VOC/pages/474742994/Sept+2026+-+HL7+WGM+-+Monday+Q4+Minutes
%Reference to experimental CodeSystem http://hl7.org/fhir/uv/smart-scheduling-links/CodeSystem/temporary%
